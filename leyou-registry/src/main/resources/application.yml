server:
  port: 10086
spring:
  application:
    name: leyou-registry

eureka:
  client:
    service-url:
      defaultZone: http://192.168.1.161:10086/eureka/
    fetch-registry: false # 是否拉取服务
  server:
    enable-self-preservation: true # 允许自我注册
    eviction-interval-timer-in-ms: 60000

